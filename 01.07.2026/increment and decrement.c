#include <stdio.h>
int main() 
{
    /* INCREMENT OPERATOR */
    int a=10;
    printf("The value of A now is %d\n\n",a);

    /* INCREMENTING OPERATOR METHODS */
    a++;
    
    printf("The value of AFTER INCREMENTING  is %d\n\n",a);
}