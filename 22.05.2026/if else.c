#include <stdio.h>
int main() 
{
    /* SIMPLE CONDITION IF ---ELSE */
    
    int A=100;
    int B=100;
    if(A>B)
        printf("A = %d\n and B=%d\n hence A is  greater than ",A,B);
    else if 
        printf("A = %d\n and B=%d\n hence A is not smaller than ",A,B);
    else if 
        printf("A =%d\n and B=%d\n hence A and B are equal ",A,B);
}